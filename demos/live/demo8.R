library(tidyverse)
library(shiny)

shinyApp(
  ui = fluidPage(
    titlePanel("Temperature Forecasts"),
    sidebarLayout(
      sidebarPanel(
        
      ),
      mainPanel( 
        
      )
    )
  ),
  server = function(input, output, session) {

  }
)
